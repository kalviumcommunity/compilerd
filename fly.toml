[build]
  image = "compilerd"
  dockerfile = "Dockerfile"

[env]
  OPENAI_API_KEY = "abc"

[services]
  [services.http]
    internal_port = 3000
    processes = ["app"]

[processes]
  app = { cmd = ["docker", "run", "-p", "3000:3000", "-e", "OPENAI_API_KEY=$OPENAI_API_KEY", "."] }
