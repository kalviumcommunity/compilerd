# App Configuration
app = "compilerd-fly"

[build]
  image = "compilerd"
  dockerfile = "Dockerfile"

[env]
  OPENAI_API_KEY = "abc"

[services]
  [services.http]
    internal_port = 3000
    processes = ["app"]

[processes]
  app = { cmd = ["sh", "/usr/bin/start.sh"] }
